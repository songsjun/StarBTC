{
  "name": "subgraph",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ bel2-loan",
    "remove-local": "graph remove --node http://localhost:8020/ bel2-loan",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bel2-loan",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ bel2-loan",
    "test": "graph test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "matchstick-as": "0.5.0"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.68.4",
    "@graphprotocol/graph-ts": "^0.33.0"
  }
}